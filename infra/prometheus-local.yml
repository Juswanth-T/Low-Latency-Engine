global:
  scrape_interval: 1s
  evaluation_interval: 1s

rule_files:
  - '/etc/prometheus/rules.yml'

scrape_configs:
  - job_name: 'rapidfeed'
    static_configs:
      - targets: ['host.docker.internal:8080']
